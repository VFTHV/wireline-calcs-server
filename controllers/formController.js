const submitProblem = async (req, res) => {
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
  res.send({ msg: 'form submitted' });
};

module.exports = { submitProblem };
